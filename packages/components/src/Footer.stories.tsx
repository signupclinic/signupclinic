import { ComponentStory } from '@storybook/react';
import Footer from './Footer';

export default {
  component: Footer,
  title: 'Footer',
};

export const Primary: ComponentStory<typeof Footer> = Footer.bind({});
